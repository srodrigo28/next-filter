import { ElementType } from "react"

interface TitleProps{
    title: string
    subTitle: string
    icon: ElementType
}
export default function Title(){
    return(
        <div>
        </div>
    )
}